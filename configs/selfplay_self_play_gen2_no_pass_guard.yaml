# Generation 2's self-play data using the full fix stack throughout: root noise
# excluding Pass (alpha=0.1), temperature annealing (temperature_drop_move=30), and the
# structural no_pass_before_move=20 guard -- chained from
# bootstrap_gen1_no_pass_guard_candidate.pt, the best checkpoint of the whole
# investigation (0/100 short games in its own generation, confirmed real strength win
# with the corrected eval, PROMOTE 1543.0 vs 1457.0).
#
# This is the actual test docs/SELF_PLAY_STABILITY.md's whole investigation has been
# building toward: does the structural guard also close the remaining chaining gap seen
# in sections 14 and 16 (6-8% short games in generation 1 climbing to 17-23% in
# generation 2), or does compounding persist even with a hard guarantee against the
# specific failure mode?

checkpoint_path: checkpoints/bootstrap_gen1_no_pass_guard_candidate.pt
board_size: 9
komi: 7.5
seed: 20260904

num_games: 100
max_moves: 200
num_simulations: 400
temperature: 1.0
temperature_drop_move: 30
no_pass_before_move: 20
max_seconds: null
root_dirichlet_epsilon: 0.25
root_dirichlet_alpha: 0.1

out_path: selfplay_games/self_play_gen2_no_pass_guard.npz
